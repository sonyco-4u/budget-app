<div class="menu_bar">
  <%= render partial: 'shared/menu', locals: { title: 'EXPENSE DETAIL', menu_path: root_path } %>
</div>

<section class="category_detail_page">
  <div class="category">
    <div>
      <% if @expense.icon.attached?%>
        <%= image_tag(@expense.icon) %>
      <%else%>
        <%= image_tag('expense.png') %>
      <%end%>
      <h2><%= @expense.name %></h2>
    </div>
    <p class="desc">Lorem ipsum dolor sit amet consectetur adipisicing elit. Eaque iste quidem repudiandae. Dolor debitis quas impedit sit officiis dolorum beatae quaerat nesciunt doloribus expedita in distinctio laborum facilis ex sunt ipsum numquam, amet et odio iusto ab est. Vero, nemo.</p>
    <h3 class="expense desc"><span>TOTAL EXPENSE: </span><span>$<%= @expense.amount %></span></h3>

    <div class="list_btn_gen">
      <button class="edit_btn list_btn"><%= link_to "Edit", edit_category_expense_path(category_id: @category.id, id: @expense.id) %></button>
      <button class="back_btn list_btn"><%= link_to "Back", category_expenses_path(category_id: @category.id) %></button>
      <%= button_to "Delete", @category_expense, method: :delete, class: 'del_btn list_btn' %>
    </div>
  </div>
</section>