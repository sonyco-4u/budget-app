databases:
  - name: Micronut
    databaseName: blogapp_bcix
    user: blogapp_bcix_user

services:
  - type: web
    name: mysite
    env: ruby
    buildCommand: './bin/render-build.sh'
    startCommand: 'bundle exec puma -C config/puma.rb'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: Myexpenses
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
